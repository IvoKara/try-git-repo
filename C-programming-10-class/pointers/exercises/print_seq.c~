#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int print_seq(int *start, int *end)
{
	for(int *i = start; i < end; i++)
		printf("%d ", *i);
}

int main(int argc, char **argv)
{
	int var;
	printf("%d\n", var);
	print_seq(&var, &var+3);
	return 0;
}
